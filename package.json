{
	"name": "portfoliopress",
	"version": "1.0.0",
	"description": "My personal portfolio project.",
	"main": "index.js",
	"scripts": {
		"watch": "browser-sync start --proxy localhost:8080 --files \"wordpress/**/*.php\" \"wordpress/**/*.css\" \"wordpress/**/*.js\"",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "docker-compose up -d && browser-sync start --proxy localhost:8080 --files \"wordpress/**/*.php\" \"wordpress/**/*.css\" \"wordpress/**/*.js\"",
		"stop": "docker-compose down"
	},
	"author": "mpk.coding@gmail.com",
	"license": "ISC"
}
